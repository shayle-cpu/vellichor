# Vellichor Design System Primitives

Foundational primitives to keep pages emotionally consistent:
- `PageShell`: mobile-first max-width container with vertical rhythm.
- `SectionCard`: default cozy surface.

Token source of truth is in `src/styles/design-system.css`.
